"""Provider matrix, resolution ladder and on-demand acquisition.

Intentionally re-exports nothing: a `resolve` function exported here would
shadow the `resolve` submodule of the same name.  Import the modules instead —
`from ..providers import matrix, resolve as resolver`.
"""
